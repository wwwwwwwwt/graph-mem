# GraphMem v0.3 Phase 2.5 Implementation Plan

> **Status**: COMPLETED (2026-05-09)
> **Version reached**: v0.3.2
> **Tests**: 72/72 passing

**Goal**: Re-rank 层 + Secret Scrubber

---

## Completion Summary

Both tasks implemented and committed to `main`.

| Task | Commit | Status |
|---|---|---|
| 1. Re-rank 层 | `7cb9fc3` | COMPLETED |
| 2. Secret Scrubber | `7cb9fc3` | COMPLETED |

---

## Task 1: Re-rank 层 ✅

**Files:**
- Create: `src/graphmem/retrieval/rerank.py`
- Modify: `src/graphmem/retrieval/search.py`
- Modify: `src/graphmem/memory.py`
- Modify: `src/graphmem/config.py`
- Test: `tests/unit/test_rerank.py`

- [x] **Step 1**: 创建 `Reranker` 类
  - 使用 `sentence-transformers.CrossEncoder`
  - 默认模型: `cross-encoder/ms-marco-MiniLM-L-6-v2` (~20MB)
  - `rerank(query, candidates, k)` 返回 top-k
  - 融合分数: 70% reranker + 30% original (vector + graph + time_decay)
- [x] **Step 2**: 修改 `SearchEngine`
  - `__init__` 接受可选 `reranker`
  - `search()` 新增 `query` 和 `top_k_multiplier` 参数
  - 有 reranker 时 vector 搜索取 k * multiplier 候选
- [x] **Step 3**: 修改 `memory.py`
  - 根据 `config.retrieval.rerank.enabled` 创建 Reranker
  - `recall()` 把 `query` 传给 SearchEngine
- [x] **Step 4**: 修改 `config.py`
  - `RerankConfig`: enabled, model, top_k_multiplier
- [x] **Step 5**: 测试 — 2/2 passed

---

## Task 2: Secret Scrubber ✅

**Files:**
- Create: `src/graphmem/retrieval/scrubber.py`
- Modify: `src/graphmem/memory.py`
- Modify: `src/graphmem/config.py`
- Test: `tests/unit/test_secret_scrubber.py`

- [x] **Step 1**: 创建 `SecretScrubber` 类
  - OpenAI key: `sk-[a-zA-Z0-9]{32,}` → `***API_KEY***`
  - Bearer token: `Bearer ...20+` → `***BEARER_TOKEN***`
  - Password: `password=...` → `***PASSWORD***`
  - Hex token: `[a-f0-9]{32}` → `***TOKEN***`
  - AWS key: `AKIA...16` → `***AWS_KEY***`
- [x] **Step 2**: 修改 `memory.py` `recall()`
  - `config.retrieval.scrub_secrets` (default True)
  - `format_results()` 后调用 `scrubber.scrub()`
- [x] **Step 3**: 修改 `config.py`
  - `retrieval.scrub_secrets: bool = True`
- [x] **Step 4**: 测试 — 6/6 passed

---

## Self-Review

| Phase 2.5 Spec Requirement | Task |
|---|---|
| Re-rank 层 (cross-encoder) | Task 1 ✅ |
| Secret Scrubber | Task 2 ✅ |

---

## What Comes Next

**Phase 3**: pre-prompt hook, L3 Reflection, 自动归档, 冲突检测
