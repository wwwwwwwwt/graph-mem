# GraphMem v0.3 Phase 2 Implementation Plan

> **Status**: COMPLETED (2026-05-09)
> **Version reached**: v0.3.1
> **Tests**: 64/64 passing

**Goal**: CLI完善、时间衰减、多跳图召回验证。

---

## Completion Summary

All 3 tasks implemented and committed to `main`.

| Task | Commit | Status |
|---|---|---|
| 1. CLI 完善 | `cf77444` | COMPLETED |
| 2. 时间衰减 | `c871152` | COMPLETED |
| 3. 多跳图召回验证 | `584ed04` | COMPLETED |

---

## Task 1: CLI 完善 ✅

**Files:**
- Modify: `src/graphmem/cli.py`
- Create: `src/graphmem/daemon/cli_entry.py`
- Test: `tests/unit/test_cli.py`

- [x] **Step 1**: 添加 `daemon start/stop/status` 子命令
  - `daemon start [--port PORT]` — subprocess 启动 uvicorn，pid 写入 `~/.graphmem/daemon.pid`
  - `daemon stop` — 读 pid 文件，kill 进程，删除 pid 文件
  - `daemon status` — 检查进程是否存活
- [x] **Step 2**: 增强 `recall` 命令
  - `--layer L1,L2` 参数
  - `--json` 参数输出 JSON
  - `--max-hops` 参数控制图扩散深度
- [x] **Step 3**: 增强 `graph` 命令
  - `--depth N` 参数
  - `--json` 参数输出 JSON
- [x] **Step 4**: 运行测试 `pytest tests/unit/test_cli.py -v` — 6/6 passed
- [x] **Step 5**: Commit `cf77444`

---

## Task 2: 时间衰减 ✅

**Files:**
- Modify: `src/graphmem/retrieval/search.py`
- Modify: `src/graphmem/memory.py`
- Modify: `src/graphmem/cli.py`
- Test: `tests/unit/test_search_time_decay.py`

- [x] **Step 1**: 在 `SearchEngine.search()` 中加入 `time_decay` 计算
  - `age_days = (now - node.created_at).total_seconds() / 86400`
  - `time_decay = math.exp(-age_days / halflife_days)` (default 30)
  - `final_score = vec_score * layer_weight * time_decay`
  - naive datetime 自动补 UTC timezone
- [x] **Step 2**: `memory.py` 的 `recall()` 把 `time_window` 参数传给 `SearchEngine.search()`
- [x] **Step 3**: 增强 CLI `recall` 的 `--since-days` 参数
- [x] **Step 4**: 运行测试 `pytest tests/unit/test_search_time_decay.py -v` — 3/3 passed
- [x] **Step 5**: Commit `c871152`

---

## Task 3: 多跳图召回验证 ✅

**Files:**
- Create: `tests/integration/test_graph_multi_hop.py`

- [x] **Step 1**: 构造 3-session 链式关系场景
  - backend_ep —MENTIONS—> PostgreSQL
  - migration_ep —MENTIONS—> PostgreSQL, Alembic
  - orm_ep —MENTIONS—> Alembic, SQLAlchemy
- [x] **Step 2**: 验证 hops=0 只有向量种子
- [x] **Step 3**: 验证 hops=1 扩展到直接邻居 (alembic, sqlalchemy)
- [x] **Step 4**: 验证 hops=2 双向 incoming 找到 migration_ep
- [x] **Step 5**: 验证 hops=3  outgoing 找到 pg
- [x] **Step 6**: 验证 hops=4 双向 incoming 找到 backend_ep
- [x] **Step 7**: Commit `584ed04`

---

## Self-Review

### Spec coverage

| Phase 2 Spec Requirement | Task |
|---|---|
| CLI daemon start/stop | Task 1 |
| recall --layer / --json | Task 1 |
| graph --depth / --json | Task 1 |
| 时间衰减 | Task 2 |
| time_window 参数打通 | Task 2 |
| 多跳图召回验证 | Task 3 |

### Gap

- Re-rank 层 → Phase 2.5
- Secret scrubber → Phase 3
- 4-session 写入速度 / 压缩质量端到端验证 → 依赖 embedding 模型精度，建议用真实 LLM (Mode B) 手动验证

---

## What Comes Next

**Phase 2.5 候选任务**:
1. Re-rank 层 (BAAI/bge-reranker-base)
2. Secret scrubber (Token 预算控制 + 敏感信息脱敏)

**Phase 3 候选任务**:
1. pre-prompt hook
2. L3 Reflection
3. 自动归档
4. 冲突检测
